<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChangeSim" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ChangeSim" parent="Base.Theme.ChangeSim" />

    <style name="Theme.ChangeSim.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <item name="appWidgetRadius">10dp</item>
        <item name="appWidgetInnerRadius">10dp</item>
    </style>

    <style name="Theme.ChangeSim.AppWidgetContainer" parent="Theme.ChangeSim.AppWidgetContainerParent">
        <item name="appWidgetPadding">0dp</item>
    </style>
</resources>